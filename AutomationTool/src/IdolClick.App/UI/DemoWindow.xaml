<Window x:Class="IdolClick.UI.DemoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="&#x1F3AC; Demo Mode &#x2014; IdolClick"
        Height="680" Width="900"
        MinHeight="450" MinWidth="650"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        Icon="/Assets/idol-click.png">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{StaticResource CardBrush}" CornerRadius="6" Padding="12,8" Margin="0,0,0,4">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="RunAllBtn" Content="&#x25B6;  Run All Demos"
                            Style="{StaticResource SmallPrimaryButton}"
                            Click="RunAll_Click" Padding="12,6" FontSize="12"/>
                    <Button x:Name="RunSelectedBtn" Content="&#x25B6;  Run Selected"
                            Style="{StaticResource SmallButton}"
                            Click="RunSelected_Click" Padding="12,6" FontSize="12" Margin="6,0,0,0"/>
                    <Button x:Name="StopBtn" Content="&#x23F9;  Stop"
                            Style="{StaticResource SmallDangerButton}"
                            Click="Stop_Click" Padding="12,6" FontSize="12" Margin="6,0,0,0"
                            IsEnabled="False"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock x:Name="SummaryText" Text="5 demos ready"
                               FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Scenario List -->
        <Border Grid.Row="1" Background="{StaticResource CardBrush}" CornerRadius="6" Padding="8" Margin="0,0,0,4">
            <ItemsControl x:Name="ScenarioList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="6,4" Margin="0,1" CornerRadius="4"
                                Background="{Binding RowBackground}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="26"/>
                                    <ColumnDefinition Width="68"/>
                                    <ColumnDefinition Width="170"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}"
                                          VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding StatusIcon}" FontSize="14"
                                           VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{Binding Id}" FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="3" Text="{Binding Name}" FontWeight="SemiBold"
                                           FontSize="12" Foreground="{StaticResource TextBrush}"
                                           VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="4" Text="{Binding Description}" FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Grid.Column="5" Text="{Binding ElapsedText}" FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center" TextAlignment="Right"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <!-- Narration Log -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="6" Margin="0,0,0,4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource CardBrush}"
                        CornerRadius="6,6,0,0" Padding="10,5">
                    <Grid>
                        <TextBlock Text="&#x1F4CB; Live Narration" FontSize="11" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="&#x1F4CB; Copy" Style="{StaticResource SmallButton}"
                                    Click="CopyLog_Click" Padding="8,2" FontSize="10"/>
                            <Button Content="&#x1F5D1; Clear" Style="{StaticResource SmallButton}"
                                    Click="ClearLog_Click" Padding="8,2" FontSize="10" Margin="4,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <TextBox x:Name="LogOutput" Grid.Row="1"
                         IsReadOnly="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         Background="{StaticResource SurfaceBrush}"
                         Foreground="{StaticResource TextBrush}"
                         BorderThickness="0" Padding="10,6"
                         FontFamily="{StaticResource MonoFont}"
                         FontSize="{StaticResource FontSizeBodySm}"/>
            </Grid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="3" Background="{StaticResource CardBrush}" CornerRadius="6" Padding="10,6">
            <TextBlock FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"
                       TextWrapping="Wrap">
                <Run Text="&#x1F4A1; "/>
                <Run Text="Demos use real Windows apps (Calculator, Notepad) and UIA automation. No LLM or API key required. " />
                <Run Text="Keep the demo window visible to watch narration while apps are automated in front of you."/>
            </TextBlock>
        </Border>
    </Grid>
</Window>
